# ContextPilot-Query-Aware-Context-Distillation-for-Efficient-RAG
ContextPilot is a RAG pipeline that scores retrieved chunks, decides whether to quote, compress, or drop them, and builds a compact evidence pack to reduce token usage while preserving grounded answer quality.
