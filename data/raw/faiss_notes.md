# FAISS Notes

FAISS is a library for efficient similarity search over dense vector representations.

It is widely used in retrieval systems to index embeddings and perform nearest-neighbor search efficiently.

In a RAG pipeline, FAISS can store document chunk embeddings and return the top-k most similar chunks for a user query.
